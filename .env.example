# 环境变量配置示例文件
# 复制此文件为 .env 或 .env.local，并填入实际的值
# 注意：不要将包含真实密码的 .env 文件提交到 Git

# ==================== 数据库配置 ====================
# 数据库主机地址（默认: localhost）
DB_HOST=localhost

# 数据库端口（默认: 3306）
DB_PORT=3306

# 数据库用户名（默认: root）
DB_USERNAME=root

# 数据库密码（必需，无默认值）
DB_PASSWORD=your_database_password_here

# ==================== Redis 配置 ====================
# Redis 主机地址（默认: localhost）
REDIS_HOST=localhost

# Redis 端口（默认: 6379）
REDIS_PORT=6379

# Redis 密码（可选，如果 Redis 没有设置密码则留空）
REDIS_PASSWORD=

# ==================== AI 服务配置 ====================
# DeepSeek API Key（必需，用于主要的 AI 模型）
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 阿里云 DashScope API Key（可选，用于智能路由）
# 获取地址: https://dashscope.console.aliyun.com/
ALIBABA_DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 阿里云 DashScope 图片生成 API Key（可选）
DASHSCOPE_IMAGE_API_KEY=your_dashscope_image_api_key_here

# Pexels API Key（可选，用于图片搜索）
# 获取地址: https://www.pexels.com/api/
PEXELS_API_KEY=your_pexels_api_key_here

# ==================== 接口文档配置（可选）====================
# Knife4j 用户名（默认: root）
KNIFE4J_USERNAME=root

# Knife4j 密码（默认: 空）
KNIFE4J_PASSWORD=
